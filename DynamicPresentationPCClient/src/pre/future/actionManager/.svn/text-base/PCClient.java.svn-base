package pre.future.actionManager;

import pre.future.Document.TranfserManager1;
import pre.future.connectionManager.PCConnectionManager;
import pre.future.pc.ui.PcClientUI;

public class PCClient {
	public static void main(String[] args) {
		// PcClientUI pcu=PcClientUI.getInstance();
		PCConnectionManager connector = new PCConnectionManager();
		connector.start();
		TranfserManager1 fm = new TranfserManager1();
	}
}